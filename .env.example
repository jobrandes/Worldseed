# Optional. If set, Worldseed uses Claude to generate rich, coherent
# world text. If left unset, Worldseed falls back to a built-in
# deterministic generator so the app still works out of the box.
ANTHROPIC_API_KEY=
